/**
 * Type the `stringify` function to take any kind of input.
 *
 * Don't use `any`!
 */
function stringify(input) {
    return `${input}`;
  }

stringify("a string");   
stringify(12);           
stringify(true);         